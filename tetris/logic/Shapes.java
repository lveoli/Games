package logic;

public class Shapes {
    public int[][] coordinates;
    public int rotation;
    public int hello;
    public String name;

    public void Shapes(int type){
        coordinates = new int[4][2];
        rotation = 0;
        int[] coord0 = new int[2];
        coord0[0] = 0;
        coord0[1] = 0;
        
        switch(type)
        {
            case 1:
                name = "T";
                coordinates[0] =
                coordinates[1] =
                coordinates[2] =
                coordinates[3] =


                break;

            case value2 :
                // Statements
                break; // break is optional

            // We can have any number of case statements
            // below is default statement, used when none of the cases is true.
            // No break is needed in the default case.
            default :
                // Statements
        }de
        coordinates
    }
}
